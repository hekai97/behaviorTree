#include "HActor.h"
class HACharacter : public HActor
{
private:
    /* data */
public:
    HACharacter(/* args */);
    ~HACharacter();
};

HACharacter::HACharacter(/* args */)
{
}

HACharacter::~HACharacter()
{
}
